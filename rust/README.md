# TwinTidy — Rust port

Rust port of TwinTidy's duplicate-detection core. Standard library only — no external crates.

## Build

```sh
cargo build --release
```

The binary lands at `target/release/twintidy`.

## Test

```sh
cargo test
```

## Usage

```text
twintidy <folder>
```
